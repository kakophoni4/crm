# Vaultwarden HTTP :80 — ACME webroot + redirect to HTTPS.

server {
    listen 80;
    server_name huitawarden.bttsrvvrs.org;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}
